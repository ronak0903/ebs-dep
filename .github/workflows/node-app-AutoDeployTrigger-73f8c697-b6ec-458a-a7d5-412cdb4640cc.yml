name: Trigger auto deployment for node-app

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/node-app-AutoDeployTrigger-73f8c697-b6ec-458a-a7d5-412cdb4640cc.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.NODEAPP_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.NODEAPP_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.NODEAPP_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: demoapp1233.azurecr.io
          registryUsername: ${{ secrets.NODEAPP_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.NODEAPP_REGISTRY_PASSWORD }}
          containerAppName: node-app
          resourceGroup: container-app
          imageToBuild: demoapp1233.azurecr.io/node-app:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


