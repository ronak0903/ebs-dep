trigger:
  - main

pool:
  vmImage: 'ubuntu-latest'

variables:
  azureServiceConnection: 'Azure-Connection-App'
  acrName: 'Demoapp1233'
  repositoryName: 'node-app'
  containerAppName: 'node-app'
  resourceGroupName: 'container-app' # Updated based on your Azure portal URL path!
  imageTag: '$(Build.BuildId)'

stages:
- stage: BuildAndDeploy
  displayName: 'Build and Deploy to Container App'
  jobs:
  - job: Build
    displayName: 'Build, Push, and Deploy'
    steps:
    # 1. Log in, Build, and Push using pipeline variables
    - task: AzureCLI@2
      displayName: 'Build and Push Docker Image to ACR'
      inputs:
        azureSubscription: '$(azureServiceConnection)'
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        inlineScript: |
          # 1. Log in securely using the pipeline secret variable
          echo $(ACR_PASSWORD) | docker login $(acrName).azurecr.io --username $(acrName) --password-stdin
          
          # 2. Build the image layers
          docker build -t $(acrName).azurecr.io/$(repositoryName):$(imageTag) -t $(acrName).azurecr.io/$(repositoryName):latest .
          
          # 3. Push both image tags to your Azure Container Registry
          docker push $(acrName).azurecr.io/$(repositoryName):$(imageTag)
          docker push $(acrName).azurecr.io/$(repositoryName):latest

    # 2. Deploy the new image revision to your Container App
    - task: AzureContainerApps@1
      displayName: 'Deploy to Azure Container Apps'
      inputs:
        azureSubscription: '$(azureServiceConnection)'
        imageToDeploy: '$(acrName).azurecr.io/$(repositoryName):$(imageTag)'
        containerAppName: '$(containerAppName)'
        resourceGroup: '$(resourceGroupName)'
